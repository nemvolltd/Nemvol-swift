// TODO: OrderDetailsModal — slide-over or centered modal showing full order breakdown.
// Sections: order ID + date, customer info, items list (image + name + qty + price), totals, status controls.
// Props: isOpen, onClose, order, onUpdateStatus

export default function OrderDetailsModal({ isOpen, onClose, order, onUpdateStatus }) {
    return null; // placeholder
}
