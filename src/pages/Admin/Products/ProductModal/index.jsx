// TODO: ProductModal — 3-step product create/edit modal (portal).
// Step 1: Info (name, category, description)
// Step 2: Pricing (price, original price, stock)
// Step 3: Media (image upload, sizes)
// Sub-components: StepIndicator, StepInfo, StepPricing, StepMedia, CategoryModal
// Props: isOpen, onClose, onSubmit, product, isLoading

export default function ProductModal({ isOpen, onClose, onSubmit, product, isLoading }) {
    return null; // placeholder
}
