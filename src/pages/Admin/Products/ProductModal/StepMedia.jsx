// TODO: StepMedia — Step 3: image uploader (multi, base64), image gallery with set-main + delete,
// and size selector (XS–XXXL toggles).
// Props: images, image, setImage, handleImageUpload, handleDeleteImage, availableSizes, selectedSizes, handleSizeToggle

export default function StepMedia(props) {
    return null; // placeholder
}
